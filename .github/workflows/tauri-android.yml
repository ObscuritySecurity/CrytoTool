name: Release (Android)

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build:
    permissions:
      contents: write
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: lts/*
          cache: 'npm'

      - name: Setup Java
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: 17

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Install Android SDK components
        run: |
          sdkmanager "platforms;android-35" "build-tools;35.0.0"

      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: aarch64-linux-android,armv7-linux-androideabi,i686-linux-android,x86_64-linux-android

      - name: Rust cache
        uses: swatinem/rust-cache@v2
        with:
          workspaces: './src-tauri -> target'

      - name: Install dependencies
        run: npm ci

      - name: Build web assets
        run: npm run build

      - name: Init Android project
        run: echo "y" | npm run tauri android init

      - name: Patch Kotlin version for biometry plugin
        run: |
          KOTLIN_FILE="src-tauri/gen/android/build.gradle.kts"
          if [ -f "$KOTLIN_FILE" ]; then
            sed -i 's/kotlin-gradle-plugin:1\.9\.[0-9]*/kotlin-gradle-plugin:2.1.0/' "$KOTLIN_FILE"
            echo "Patched Kotlin in $KOTLIN_FILE"
            grep 'kotlin-gradle-plugin' "$KOTLIN_FILE"
          fi

      - name: Generate app icons
        run: npm run tauri -- icon src-tauri/icon-sources/icon-manifest.json -o src-tauri/icons

      - name: Build Universal APK
        run: npm run tauri android build -- --apk

      - name: Find APK files
        run: |
          echo "=== Finding APKs ==="
          find src-tauri/gen/android/app/build/outputs/apk -name "*.apk" -type f

      - name: Generate debug keystore
        env:
          KEYSTORE_PASS: ${{ secrets.ANDROID_KEYSTORE_PASSWORD || 'crytotool123' }}
        run: |
          keytool -genkey -v -keystore release.keystore \
            -alias crytotool -keyalg RSA -keysize 2048 -validity 10000 \
            -storepass "$KEYSTORE_PASS" -keypass "$KEYSTORE_PASS" \
            -dname "CN=CrytoTool, O=CrytoTool, C=US"

      - name: Sign APK
        env:
          KEYSTORE_PASS: ${{ secrets.ANDROID_KEYSTORE_PASSWORD || 'crytotool123' }}
        run: |
          BUILD_TOOLS="${ANDROID_HOME}/build-tools/35.0.0"
          
          UNSIGNED=$(find src-tauri/gen/android/app/build/outputs/apk -name "*-release-unsigned.apk" | head -1)
          
          if [ -n "$UNSIGNED" ]; then
            ALIGNED="${UNSIGNED%.apk}-aligned.apk"
            SIGNED="${UNSIGNED%.apk}-signed.apk"
            
            "$BUILD_TOOLS/zipalign" -v -p 4 "$UNSIGNED" "$ALIGNED"
            "$BUILD_TOOLS/apksigner" sign \
              --ks release.keystore \
              --ks-pass pass:"$KEYSTORE_PASS" \
              --key-pass pass:"$KEYSTORE_PASS" \
              --out "$SIGNED" "$ALIGNED"
            
            rm -f "$ALIGNED"
            cp "$SIGNED" CrytoTool-android.apk
            echo "=== Signed APK: CrytoTool-android.apk ==="
            ls -la CrytoTool-android.apk
          else
            echo "No unsigned APK found!"
            find src-tauri/gen/android/app/build/outputs/apk -name "*.apk"
          fi

      - name: Get version
        id: version
        run: echo "version=${GITHUB_REF#refs/tags/v}" >> "$GITHUB_OUTPUT"

      - name: Upload to GitHub Release
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v3
        with:
          tag_name: ${{ github.ref_name }}
          draft: true
          files: CrytoTool-android.apk

      - name: Upload artifact
        if: ${{ !startsWith(github.ref, 'refs/tags/v') }}
        uses: actions/upload-artifact@v4
        with:
          name: android-apk
          path: CrytoTool-android.apk